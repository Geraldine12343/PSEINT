Definición de Clases:

Sintaxis para definir una clase.
Variables de instancia y métodos de instancia.
Métodos especiales (__init__, __str__, __repr__, etc.).
Uso de la palabra clave self.
Objetos y Instancias:

Creación de objetos a partir de una clase.
Instancias y atributos de instancia.
Acceso a atributos y métodos de instancia.
Herencia:

Creación de clases derivadas (subclases) a partir de clases base (superclases).
Sobreescritura de métodos.
Uso de super() para llamar métodos de la clase base.
Encapsulamiento:

Public, protected y private attributes (Atributos públicos, protegidos y privados).
Métodos y atributos protegidos y privados.
Polimorfismo:

Uso de interfaces y clases abstractas.
Duck typing y funciones polimórficas.
Propiedades y Decoradores:

Propiedades (getters y setters).
Uso de decoradores (@property, @staticmethod, @classmethod).
Métodos Especiales (Magic Methods o Dunder Methods):

__init__: Método constructor.
__str__ y __repr__: Representación en cadena del objeto.
__add__: Sobrecarga de operadores.

Uso de Clases:
Clases abstractas y herencia múltiple.